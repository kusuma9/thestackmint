# Generate: openssl rand -hex 32
VAULTWARDEN_ADMIN_TOKEN=replace_with_strong_random_token
# SMTP (optional, for email verification)
SMTP_HOST=
SMTP_FROM=vaultwarden@mystackmint.com
SMTP_USERNAME=
SMTP_PASSWORD=
