# Homepage environment variables
# Copy to .env — this file is safe to commit (no real secrets)

# Set to your Uptime Kuma status page slug to enable live status dots
# The nginx.conf proxies /api/status → http://uptime-kuma:3001/api/status-page/heartbeat/<slug>
# Leave empty to disable status polling
UPTIME_KUMA_SLUG=default
