# CrowdSec bouncer API key — registered on container start via BOUNCER_KEY_nginx env var
# Choose a strong random string (e.g. openssl rand -hex 32)
CROWDSEC_BOUNCER_API_KEY=your_crowdsec_bouncer_api_key_here
